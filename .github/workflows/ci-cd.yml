name: CI/CD

on:
  push:
    branches: [ main, staging ]

env:
  IMAGE_NAME: demo-django

jobs:

  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      image-tag: ${{ steps.meta.outputs.image-tag }}

    steps:
    - uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Build local Docker image (no push required for KIND)
      id: meta
      run: |
        docker build -t $IMAGE_NAME:${{ github.sha }} -f django-app/Dockerfile django-app
        echo "image-tag=$IMAGE_NAME:${{ github.sha }}" >> $GITHUB_OUTPUT


  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: staging

    steps:
    - uses: actions/checkout@v4

    - name: Create KIND Cluster
      uses: helm/kind-action@v1.8.0

    - name: Build Docker image inside deploy job
      run: |
        docker build -t demo-django:${{ github.sha }} -f django-app/Dockerfile django-app

    - name: Load image into KIND
      run: |
        kind load docker-image demo-django:${{ github.sha }}

    - name: Apply Kubernetes manifests
      run: |
        sed -i "s|demo-django:local|demo-django:${{ github.sha }}|g" k8s/deployment.yaml
        kubectl apply -f k8s/

    - name: Wait for rollout
      run: kubectl rollout status deployment/demo-django --timeout=90s

    - name: Smoke test
      run: |
        kubectl port-forward svc/demo-django 8080:80 >/dev/null 2>&1 &
        sleep 4
        if ! curl -fsS http://127.0.0.1:8080/health; then
          echo "SMOKE TEST FAILED"
          exit 1
        fi


  rollback:
    runs-on: ubuntu-latest
    needs: deploy
    if: failure()

    steps:
    - name: Create KIND Cluster
      uses: helm/kind-action@v1.8.0

    - name: Rollback Deployment
      run: |
        kubectl rollout undo deployment/demo-django || true
        kubectl rollout status deployment/demo-django --timeout=90s || true

    - name: Slack Notification
      uses: slackapi/slack-github-action@v1.24.0
      with:
        payload: |
          {
            "text": "🚨 *Automatic Rollback Executed*\nDeployment failed during CI/CD pipeline. KIND cluster has been reverted to the last stable version."
          }
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
